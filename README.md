# fastflow_golang
untuk backend golang

init swagger refresh
`swag init -g main.go -o docs`

kalau masih bandel paksa
` swag init -g main.go -d . --parseDependency --parseInternal`

menjalankan program
1. cd dir 'project_be'
2. ` go run .`